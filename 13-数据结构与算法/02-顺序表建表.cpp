/*
int A[maxSize];
int length;
int createList(int A[], int &length)
{
    cin >> length;
    if (length > maxSize)
        return 0;
    for (int i = 0; i < length; ++i)
        cin >> A[i];
    return 1;
*/